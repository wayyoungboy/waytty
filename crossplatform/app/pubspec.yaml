name: yourssh
description: waytty - Cross-platform SSH, SFTP, Telnet and terminal workspace for macOS, Windows and Android tablets.
publish_to: 'none'
version: 0.0.3+4

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  waytty_l10n:
    path: ../packages/waytty_l10n

  # SSH / SFTP / Port forwarding
  dartssh2: ^2.9.0

  # Terminal emulator widget
  xterm: ^4.0.0

  # PTY (pseudo-terminal) for local shell sessions
  flutter_pty: ^0.4.2

  # Desktop native serial ports; Android uses the app's USB Host channel.
  flutter_libserialport: ^0.6.0

  # Secure credential storage (Keychain on macOS, Credential Manager on Windows)

  # State management
  provider: ^6.1.2

  # SVG rendering for OS icons
  flutter_svg: ^2.0.10

  # App settings persistence
  shared_preferences: ^2.3.2

  # File picker for SFTP upload
  file_picker: ^8.1.2

  # File selector for "Open with... Choose…" app picker
  file_selector: ^0.9.0

  # QR code generation (P2P sync)
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.2.3
  local_auth: ^2.3.0

  # UUID generation
  uuid: ^4.5.1

  # Path utilities
  path: ^1.9.0
  path_provider: ^2.1.4

  # Hotkey management
  hotkey_manager: ^0.2.3

  # Window management
  window_manager: ^0.3.9

  # System notifications
  local_notifier: ^0.1.6

  # WebView for code editor
  webview_flutter: ^4.8.0

  # Held at 2.12.x by the lockfile on purpose (#96): 2.13+ depends on passkeys,
  # whose passkeys_darwin plugin needs macOS 13.5 while the Runner targets 10.15,
  # which breaks `flutter build macos`. Bumping this means raising
  # MACOSX_DEPLOYMENT_TARGET in the same change.


  # Standalone account API and HTTP integrations
  http: ^1.2.0

  # Plugin system
  yourssh_plugin_api:
    path: ../packages/yourssh_plugin_api
  yourssh_devops:
    path: ../packages/yourssh_devops
  yourssh_web_tools:
    path: ../packages/yourssh_web_tools
  yourssh_snippets:
    path: ../packages/yourssh_snippets
  yourssh_script_engine:
    path: ../packages/yourssh_script_engine
  yourssh_rdp:
    path: ../packages/yourssh_rdp
  yourssh_vnc:
    path: ../packages/yourssh_vnc

  # Cryptography utilities
  cryptography: ^2.7.0

  # Network info (LAN share)
  network_info_plus: ^6.0.0

  # mDNS/Bonjour device discovery
  multicast_dns: ^0.3.2

  # HTTP server (LAN share)
  shelf: ^1.4.1

  # Markdown rendering (AI chat)
  flutter_markdown: ^0.7.1

  # HMAC/SHA hashing for AWS Signature V4 (S3 browser)
  crypto: ^3.0.3

  # AccumulatorSink<Digest> for streaming SHA-256 verification in UpdateService
  convert: ^3.1.1

  # Audit log storage (direct SQL, no codegen)
  sqlite3: ^2.4.0
  # Bundles the native sqlite3 library on macOS/Windows/Linux
  sqlite3_flutter_libs: ^0.5.20

  # FFI utilities (Utf16 string conversion, Arena allocator) — Windows SSH agent named pipe
  ffi: ^2.1.3

  # XML parsing for S3 API responses
  xml: ^6.5.0

  # URL launcher (Cloudflare tunnel)
  url_launcher: ^6.3.0

  package_info_plus: ^9.0.1

  # Google Fonts (Manrope, Inter, Roboto Mono) for the mobile UI
  google_fonts: ^6.2.1


dependency_overrides:
  # Local fork of flutter_libserialport 0.6.0: upstream android/build.gradle
  # still calls jcenter(), which Gradle 9 / AGP 9 removed. Desktop serial uses
  # this package; Android runtime uses the app SerialChannel instead, but the
  # plugin is still resolved for all platforms.
  flutter_libserialport:
    path: ../packages/flutter_libserialport
  dartssh2:
    path: ../packages/dartssh2
  # Local fork: fixes Windows argv duplication (upstream flutter_pty #19) that
  # broke keyboard input in the local PowerShell terminal.
  flutter_pty:
    path: ../packages/flutter_pty
  # Local fork: passes the hosting viewId to TextInputConfiguration (upstream
  # xterm.dart #207) — without it the Windows engine rejects the text-input
  # client and printable keys never reach any TerminalView.
  xterm:
    path: ../packages/xterm
  yourssh_script_engine:
    path: ../packages/yourssh_script_engine

dev_dependencies:
  webview_flutter_platform_interface: ^2.15.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_async: ^1.3.1
  analyzer: ^10.1.0

# Launcher icons are NOT generated by flutter_launcher_icons: it only covers
# Android/iOS, takes a raster input, and cannot do the per-platform insets the
# macOS icon grid needs. Every target is rendered from the vector masters by
# `python3 tool/gen_app_icons.py` instead.

flutter:
  uses-material-design: true
  assets:
    - assets/monaco_editor.html
    - assets/app_icon.png
    - assets/os/
    - assets/serial-licenses/
    - assets/bundled_plugins/snippets/plugin.json
    - assets/bundled_plugins/snippets/index.js
    - assets/bundled_plugins/snippets/panel/index.html
  fonts:
    - family: DejaVu Sans Mono for Powerline
      fonts:
        - asset: assets/fonts/powerline/DejaVu Sans Mono for Powerline.ttf
    - family: Inconsolata for Powerline
      fonts:
        - asset: assets/fonts/powerline/Inconsolata for Powerline.otf
    - family: Meslo LG S for Powerline
      fonts:
        - asset: assets/fonts/powerline/Meslo LG S Regular for Powerline.ttf
    - family: Source Code Pro for Powerline
      fonts:
        - asset: assets/fonts/powerline/Source Code Pro for Powerline.otf
    - family: Ubuntu Mono derivative Powerline
      fonts:
        - asset: assets/fonts/powerline/Ubuntu Mono derivative Powerline.ttf
    - family: Roboto Mono for Powerline
      fonts:
        - asset: assets/fonts/powerline/Roboto Mono for Powerline.ttf
    # Italic faces dropped (-4.8 MB): terminals rarely hit italic, and the
    # engine falls back to a synthetic slant of the upright faces.
    - family: MesloLGS NF
      fonts:
        - asset: assets/fonts/nerd/MesloLGS NF Regular.ttf
        - asset: assets/fonts/nerd/MesloLGS NF Bold.ttf
          weight: 700
