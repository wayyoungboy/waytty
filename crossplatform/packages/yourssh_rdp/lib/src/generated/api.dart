// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'api.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `runtime`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `RemoveOnDrop`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `clone`, `drop`

Future<String> rdpLibVersion() => RustLib.instance.api.crateApiRdpLibVersion();

Stream<RdpEvent> rdpConnect({required RdpConfig config}) =>
    RustLib.instance.api.crateApiRdpConnect(config: config);

Future<void> rdpDisconnect({required int sessionId}) =>
    RustLib.instance.api.crateApiRdpDisconnect(sessionId: sessionId);

Future<void> rdpSendMouse({
  required int sessionId,
  required int x,
  required int y,
  required int button,
  required int action,
}) => RustLib.instance.api.crateApiRdpSendMouse(
  sessionId: sessionId,
  x: x,
  y: y,
  button: button,
  action: action,
);

Future<void> rdpSendWheel({
  required int sessionId,
  required int delta,
  required bool horizontal,
}) => RustLib.instance.api.crateApiRdpSendWheel(
  sessionId: sessionId,
  delta: delta,
  horizontal: horizontal,
);

Future<void> rdpSendKey({
  required int sessionId,
  required int scancode,
  required bool extended,
  required bool down,
}) => RustLib.instance.api.crateApiRdpSendKey(
  sessionId: sessionId,
  scancode: scancode,
  extended: extended,
  down: down,
);

Future<void> rdpSendClipboardText({
  required int sessionId,
  required String text,
}) => RustLib.instance.api.crateApiRdpSendClipboardText(
  sessionId: sessionId,
  text: text,
);

class RdpCertInfo {
  final String sha256Fingerprint;
  final String subject;

  const RdpCertInfo({required this.sha256Fingerprint, required this.subject});

  @override
  int get hashCode => sha256Fingerprint.hashCode ^ subject.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RdpCertInfo &&
          runtimeType == other.runtimeType &&
          sha256Fingerprint == other.sha256Fingerprint &&
          subject == other.subject;
}

class RdpConfig {
  final String targetHost;
  final int targetPort;
  final String username;
  final String password;
  final String? domain;
  final int width;
  final int height;

  /// "auto" | "nla" | "tls"
  final String security;

  /// Previously pinned SHA-256 fingerprint of the server public key.
  /// When set, the TLS handshake result is compared BEFORE CredSSP runs —
  /// a mismatch aborts the connection without ever sending credentials.
  final String? expectedFingerprint;

  const RdpConfig({
    required this.targetHost,
    required this.targetPort,
    required this.username,
    required this.password,
    this.domain,
    required this.width,
    required this.height,
    required this.security,
    this.expectedFingerprint,
  });

  @override
  int get hashCode =>
      targetHost.hashCode ^
      targetPort.hashCode ^
      username.hashCode ^
      password.hashCode ^
      domain.hashCode ^
      width.hashCode ^
      height.hashCode ^
      security.hashCode ^
      expectedFingerprint.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RdpConfig &&
          runtimeType == other.runtimeType &&
          targetHost == other.targetHost &&
          targetPort == other.targetPort &&
          username == other.username &&
          password == other.password &&
          domain == other.domain &&
          width == other.width &&
          height == other.height &&
          security == other.security &&
          expectedFingerprint == other.expectedFingerprint;
}

@freezed
sealed class RdpEvent with _$RdpEvent {
  const RdpEvent._();

  /// Always the first event. Carries session id (FRB discards Rust return
  /// value of StreamSink-taking functions — issue #2233).
  const factory RdpEvent.started({required int sessionId}) = RdpEvent_Started;

  /// Connection fully established. Carries the server-negotiated desktop
  /// size, which may differ from the requested one — the Dart framebuffer
  /// must be sized from these values, not the request.
  const factory RdpEvent.connected({
    required RdpCertInfo cert,
    required int desktopWidth,
    required int desktopHeight,
  }) = RdpEvent_Connected;

  /// The server's certificate does not match `expected_fingerprint`.
  /// Emitted before any credentials are sent; the session then aborts.
  const factory RdpEvent.certMismatch({required String fingerprint}) =
      RdpEvent_CertMismatch;
  const factory RdpEvent.frameUpdate({
    required int x,
    required int y,
    required int width,
    required int height,
    required Uint8List rgba,
  }) = RdpEvent_FrameUpdate;
  const factory RdpEvent.clipboardText({required String text}) =
      RdpEvent_ClipboardText;
  const factory RdpEvent.disconnected({required String reason}) =
      RdpEvent_Disconnected;
  const factory RdpEvent.error({required String message}) = RdpEvent_Error;
}
